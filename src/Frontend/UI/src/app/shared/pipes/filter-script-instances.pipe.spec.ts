/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterScriptInstancesPipe } from './filter-script-instances.pipe';

describe('Pipe: FilterScriptInstance', () => {
  it('create an instance', () => {
    let pipe = new FilterScriptInstancesPipe();
    expect(pipe).toBeTruthy();
  });
});
