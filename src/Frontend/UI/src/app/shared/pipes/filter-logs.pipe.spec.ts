import { FilterLogsPipe } from './filter-logs.pipe';

describe('FilterLogsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLogsPipe();
    expect(pipe).toBeTruthy();
  });
});
