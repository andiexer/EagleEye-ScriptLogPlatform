import { LogLevelIconPipe } from './log-level-icon.pipe';

describe('LogLevelIconPipe', () => {
  it('create an instance', () => {
    const pipe = new LogLevelIconPipe();
    expect(pipe).toBeTruthy();
  });
});
